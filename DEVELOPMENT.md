# dailyOS 개발 가이드라인

이 문서는 dailyOS 프로젝트의 합의된 개발 관행을 설명하며, 브랜치 전략과 커밋 컨벤션을 다룹니다.

## 1. 브랜치 전략

프로젝트는 지속적인 개발 및 통합에 맞춰 Git Flow와 유사한 브랜칭 모델을 따릅니다.

*   **`main` 브랜치**:
    *   프로덕션 배포 준비가 완료된 코드를 나타냅니다.
    *   안정적이고 완전히 테스트된 릴리스만 `main`으로 병합됩니다.
    *   `main`에 대한 직접 커밋은 엄격히 금지됩니다.
    *   릴리스는 `main`에서 태그됩니다.

*   **`develop` 브랜치**:
    *   진행 중인 개발을 위한 통합 브랜치 역할을 합니다.
    *   모든 새로운 기능 및 버그 수정은 먼저 `develop`으로 병합됩니다.
    *   이 브랜치는 항상 배포 가능한 상태여야 하지만, 반드시 프로덕션 준비가 완료된 상태일 필요는 없습니다.

*   **기능 브랜치 (`feat/<기능-이름>`)**:
    *   새로운 기능을 개발하기 위해 `develop`에서 생성됩니다.
    *   명명 규칙: `feat/<설명적인-기능-이름>` (예: `feat/통합-캘린더-뷰`).
    *   작고 단일 기능에 집중해야 합니다.
    *   개발이 완료되고 검토되면 `develop`으로 다시 병합됩니다.

*   **버그 수정 브랜치 (`fix/<버그-설명>`)**:
    *   개발 또는 테스트 중에 발견된 버그를 수정하기 위해 `develop`에서 생성됩니다.
    *   명명 규칙: `fix/<설명적인-버그-이름>` (예: `fix/캘린더-이벤트-표시-문제`).
    *   수정이 구현되고 검증되면 `develop`으로 다시 병합됩니다.

*   **핫픽스 브랜치 (`hotfix/<핫픽스-설명>`)**:
    *   즉시 프로덕션에 배포해야 하는 긴급 버그 수정을 위해 `main`에서 생성됩니다.
    *   명명 규칙: `hotfix/<설명적인-핫픽스-이름>` (예: `hotfix/치명적인-인증-버그`).
    *   수정 후 `main` (즉시 배포용)과 `develop` (향후 릴리스에 수정 사항 포함 보장용) 모두에 병합됩니다.

## 2. 커밋 컨벤션

프로젝트는 Conventional Commits 사양을 준수합니다. 이는 명시적인 커밋 히스토리를 제공하여 자동화된 도구를 작성하고, 히스토리를 이해하며, 변경 로그를 생성하는 것을 더 쉽게 만듭니다.

**형식**: `<타입>: <제목>`

**예시**: `feat: 라이프 시간 장소 축 통합`

**타입**:

*   **`feat`**: 새로운 기능. (예: `feat: 사용자 로그인 기능 추가`)
*   **`fix`**: 버그 수정. (예: `fix: 캘린더 날짜 선택 오류 수정`)
*   **`docs`**: 문서만 변경. (예: `docs: README.md 업데이트`)
*   **`style`**: 코드의 의미에 영향을 주지 않는 변경 (공백, 포맷팅, 누락된 세미콜론 등). (예: `style: 코드 포맷팅 적용`)
*   **`refactor`**: 버그를 수정하지도 않고 기능을 추가하지도 않는 코드 변경. (예: `refactor: 인증 로직 개선`)
*   **`perf`**: 성능을 향상시키는 코드 변경. (예: `perf: 이미지 로딩 최적화`)
*   **`test`**: 누락된 테스트 추가 또는 기존 테스트 수정. (예: `test: 사용자 인증 테스트 추가`)
*   **`chore`**: 소스 또는 테스트 파일을 수정하지 않는 기타 변경. (예: `chore: 빌드 스크립트 업데이트`)
*   **`build`**: 빌드 시스템 또는 외부 종속성에 영향을 미치는 변경 (예시 범위: gulp, broccoli, npm). (예: `build: package.json 의존성 업데이트`)
*   **`ci`**: CI 구성 파일 및 스크립트 변경 (예시 범위: Travis, Circle, BrowserStack, SauceLabs). (예: `ci: GitHub Actions 설정 추가`)
*   **`revert`**: 이전 커밋 되돌리기. (예: `revert: "feat: 불필요한 기능 제거"`)

**제목**:

*   소문자로 시작합니다.
*   명령형, 현재 시제를 사용합니다: "change" (변경)이지 "changed" (변경됨) 또는 "changes" (변경 사항)가 아닙니다.
*   끝에 마침표를 찍지 않습니다.
*   간결하게 유지합니다.
*   메시지는 한국어로 작성합니다.

## 3. 변경 사항 워크플로우

1.  `develop`에서 **새 브랜치를 생성합니다** (예: `git checkout -b feat/my-new-feature develop`).
2.  변경 사항을 구현합니다.
3.  **커밋 컨벤션**에 따라 변경 사항을 커밋합니다.
4.  브랜치를 원격 저장소에 푸시합니다.
5.  `develop`으로 병합하기 위한 Pull Request (PR)를 생성합니다.
6.  검토 및 승인 후 PR을 병합합니다.
7.  긴급한 프로덕션 수정 사항의 경우 핫픽스 브랜치 전략을 따릅니다.

## 4. 사용자 검토 프로세스

에이전트가 변경 사항을 구현한 후, 사용자는 브라우저에서 이를 검토합니다. 사용자가 확인하면 에이전트는 해당 브랜치에 변경 사항을 커밋하는 작업을 진행합니다.
